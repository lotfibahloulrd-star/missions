<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /missions/
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-l
  RewriteRule . /missions/index.html [L]
</IfModule>
